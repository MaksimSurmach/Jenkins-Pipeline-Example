apiVersion: apps/v1
kind: Deployment
metadata:
  name: helloworld
  namespace: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: helloworld
  template:
    metadata:
      labels:
        app: helloworld
    spec:
      imagePullSecrets:
      - name: gcr-json-key
      containers:
        - name: helloworld
          image: gcr.io/personal-342719/helloworld-msurmach:rc-<BUILD>
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: helloworld-svc
  namespace: prod
spec:
  type: NodePort
  selector:
    app: helloworld
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30001

